<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interview.mapper.InterviewSessionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="s.id">
        INSERT INTO interview_sessions (user_id, resume_id, jd_id, session_type, status, mode, greeting, greeting_audio_url)
        VALUES (#{s.userId}, #{s.resumeId}, #{s.jdId}, #{s.sessionType}, #{s.status}, #{s.mode}, #{s.greeting}, #{s.greetingAudioUrl})
    </insert>

</mapper>
